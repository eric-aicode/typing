# NativeCamp 英文聽打系統 - 開發定稿與技術規格書

本文件統整了「NativeCamp 英文聽打系統」的最終開發版本規範，涵蓋視覺設計、核心邏輯、學習模式及快捷鍵操作，作為後續維護與功能擴充的依據。

---

## 1. 課程卡片 (Lesson Cards) 視覺規範

### 標題與資訊提取
- **首句標題**：卡片中央大標題自動抓取文章「第一行內容」，並自動去除末尾標點符號（`.!?`），確保留白美感。
- **課次代號 (L#)**：右上角顯示縮寫如 `L1`, `L2`。邏輯優先從 JSON `title` 欄位提取數字，若無則取 ID 末位。
- **等級標籤**：
    - **Beginner (初級)**：科技藍 (`#6366f1`)。
    - **Intermediate (中級)**：琥珀橘 (`#fbbf24`)。

### 視覺輔助元件
- **側邊色彩條**：左側垂直色彩條與**文章等級**同步（不再反映學習進度），幫助使用者快速區分難度。
- **音速/音檔圖示**：左下角改為「音符圖示 `🎶`」，其發光顏色（Blue/Amber Glow）與等級顏色保持一致。
- **版面優化**：縮短了頂部間距，使卡片結構更緊湊。

---

## 2. 核心打字機制 (Typing Engine)

### 防止長度暗示 (Anti-Hinting)
- **統一寬度**：每一句練習中的所有輸入框，皆會取該句「最長單字」的長度作為統一固定寬度。
- **文字置中**：所有輸入字體中心對齊，消除字元數量對視覺造成的提示。

### 檢查邏輯 (Checking Logic)
- **按 Enter 才檢查**：輸入過程中不進行即時判定，允許使用者完整輸入思緒。按下 `Enter` 鍵後，系統會同步校驗「整句」所有單字。
- **錯誤回饋**：
    - 若有單字錯誤，輸入框變紅，且整個輸入區塊會產生「劇烈震動 (Shake)」效果。
    - 正確單字會鎖定（Disabled）並變為綠色。
- **成功效能**：全句正確時觸發彩色噴紙花 (Confetti) 與成功提示音，隨後自動切換至下一句。

---

## 3. 學習模式 (Learning Modes)

- **新手模式 (Novice)**：
    - 輸入框顯示半透明的「完整單字」作為底稿 (`opacity: 0.45`)，使用者可進行「疊打」練習。
- **專家模式 (Expert)**：
    - 佔位符統一代稱為 `...`，不顯示任何首字母或字數限制，完全依賴聽力。
- **學徒模式 (Trainee)**：
    - 顯示首字母與對應長度的底線（例如 `H____`）。

---

## 4. 交互體驗與快捷鍵 (Interaction & Shortcuts)

### 鍵盤導航 (Manual Navigation)
- **空格鍵 (`Space`)**：向後跳轉至下一個單字框，並自動選取內容。
- **退格鍵 (`Backspace`)**：在框內為空時按下，自動回溯至上一個單字框。
- **Tab 鍵**：在所有輸入框之間循環切換。

### 功能快捷鍵
- **左邊 CTRL (`Left CTRL`)**：重新播當前句音檔。
- **右邊 CTRL (`Right CTRL`)**：**框內閃現提示**。正確答案會直接在當下輸入框閃現 0.8 秒後消失，輔助記憶。
- **[ / ]**：調整語音播放速度（±0.1x）。

### 視覺與感官
- **虛擬按鍵特效**：畫面頂部的虛擬鍵盤與實體鍵盤同步，亮起藍色微光並有「下壓」動態感。
- **機械音效**：專業級多層次音效（撞擊聲 + 共振聲），覆蓋所有按鍵（含 Space/Enter/Backspace）。

---

## 5. 技術結構摘要

- **前端框架**：Vanilla JS + CSS3 (Glassmorphism)。
- **持久化**：使用 `localStorage` 儲存「教材庫 (nc_library)」與「統計數據 (dictation_stats)」。
- **音訊驅動**：利用 Web Audio API (AudioContext) 動態合成機械音效，確保毫秒級無延遲觸發。

---
*文件更新日期：2026-02-10*
*Developer: Antigravity AI*
